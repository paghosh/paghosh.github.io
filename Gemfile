source "https://rubygems.org"

# GitHub Pages builds this site natively with the github-pages gem (Jekyll 3.10).
# Locally on a machine with Ruby 3.x, run: bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick"
